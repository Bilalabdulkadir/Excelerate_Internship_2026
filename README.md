# Excelerate_Internship_2026
# Add your files git add .  # Create your first commit git commit -m "Initial commit"  # Push to GitHub (this creates the main/master branch on GitHub) git branch -M main git push -u origin main
